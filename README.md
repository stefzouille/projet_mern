# projet_mern

project initialisation :
npm init -y
npm i --save express nodemon dotenv
npm i -s mongoose

start project in terminal :
npm start

you do create a .env file in the project folder with the following content :

PORT=place here your port
DB_USER_PASS=place here your db user and password
